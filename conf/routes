# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET   /health                     com.letgo.twitter.web.controllers.HealthController.healthCheck()
GET   /twitter/:username/tweets   com.letgo.twitter.web.controllers.TweetsController.findTweets(username: String, page_size: Integer?=10)